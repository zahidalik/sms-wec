.is-size-4.has-text-centered.has-background-light All Classes
%table.table.is-fullwidth.is-hoverable.has-background-info-light
  %thead
    %tr
      %th{:scope => "col"} School Related
      %th{:scope => "col"} Admin Action
      - if current_user.has_role?(:admin) && params[:user_id]
        %th{:scope => "col"} Create User Account
  %tbody
    %tr
      %th{:scope => "row"} Classes
      %td
        = link_to school_standards_url(@school) do
          Show all classes
    %tr
      %th{:scope => "row"} Users
      %td
        = link_to school_users_url(@school) do
          Show all Users
    %tr
      %th{:scope => "row"} Students
      %td
        = link_to school_students_url(@school) do
          Show all Students
      -# %td
        -# = link_to school_standards_path(school), class: "btn btn-sm btn-primary" do
        -#   Classes in
        -#   = school.name
        -# - if current_user.has_role?(:admin) && params[:user_id]
        -#   = link_to new_user_standard_user_standard_academic_year_url(@user, standard), class: "button is-link" do
        -#     "Click to add the user to this Class"